import React from 'react'

function ProfileTutorSubjects({profile, classesProfile, classesSelect}) {
    return (
        <div className={classesProfile.container}>
            
        </div>
    )
}

export default ProfileTutorSubjects
